package events

// MakeEventBus will return a fully loaded (with EventRoutes)
// eventbus. This function won't be dynamic, need to hardcode all
// the EventRoutes here.
func MakeBus() Bus {
	var eb EventBus
	return eb
}
